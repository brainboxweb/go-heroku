deployment:

  staging:
    branch: master
    commands:
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      # - git push -f git@heroku.com:farepilot-services-staging.git $CIRCLE_SHA1:refs/heads/master
      - git push -f git@heroku.com:goforfun.git $CIRCLE_SHA1:refs/heads/master


  # production:
  #   branch: production
  #   commands:
  #     - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
  #     - git push -f git@heroku.com:farepilot-services-production.git $CIRCLE_SHA1:refs/heads/master
